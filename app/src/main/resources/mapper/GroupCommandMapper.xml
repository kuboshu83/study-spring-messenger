<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.manager.infrastructure.repository.GroupCommandDAO">
    <insert id="save" parameterType="org.example.manager.infrastructure.repository.GroupDTO">
        INSERT INTO groups (id, name, locked, description)
        values (CAST(#{id} AS uuid), #{name}, #{locked}, #{description})
    </insert>

    <delete id="deleteByGroupId">
        DELETE FROM groups WHERE id=CAST(#{groupId} AS uuid)
    </delete>

    <update id="update" parameterType="org.example.manager.infrastructure.repository.GroupDTO">
        UPDATE groups SET name=#{name}, locked=#{locked}, description=#{description} WHERE id=CAST(#{id} AS uuid)
    </update>
</mapper>