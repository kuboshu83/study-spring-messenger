<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.infrastructure.repository.ApplicationQueryDAO">
    <select id="findAll" resultType="org.example.infrastructure.repository.ApplicationDTO">
        SELECT
        A.id AS applicationId,
        A.name,
        A.locked,
        B.group_id AS groupId,
        A.description
        FROM applications AS A
        LEFT JOIN applications_groups AS B
        ON A.id = B.application_id
    </select>

    <select id="findByApplicationName" resultType="org.example.infrastructure.repository.ApplicationDTO">
        SELECT
        A.id AS applicationId,
        A.name,
        A.locked,
        B.group_id AS groupId,
        A.description
        FROM applications AS A
        LEFT JOIN applications_groups AS B
        ON A.id = B.application_id
        WHERE A.name=#{applicationName}
    </select>
</mapper>